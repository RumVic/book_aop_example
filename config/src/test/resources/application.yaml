server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: file:///Users/vicrum/IDEA_PROJECTS/book-config-server-testing
          default-label: master
          clone-on-start: true
logging:
  level:
    org.springframework.cloud: DEBUG


