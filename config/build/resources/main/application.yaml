# application.yml
server:
    port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RumVic/book-config-server
          default-label: master
          clone-on-start: true
logging:
  level:
    org.springframework.cloud: DEBUG





